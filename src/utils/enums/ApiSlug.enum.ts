export enum ApiSlug {
  SIGN_UP = "/auth/register",
  LOGIN = "/auth/login",
  GOOGLE_LOGIN = "/auth/google/login",
  REFRESH_TOKEN = "/auth/refresh",
  USERS = "/users",
  ACTIVATE = "auth/activate",
}
